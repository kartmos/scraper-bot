version: "3.9"

services:
  bot-insta:
    platform: linux/amd64
    build:
      context: ../../
      dockerfile: build/deploy/Dockerfile
    container_name: bot
    image: opezdal-beta

    restart: unless-stopped